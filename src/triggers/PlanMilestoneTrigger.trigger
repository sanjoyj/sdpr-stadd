trigger PlanMilestoneTrigger on Plan_Milestone__c (after insert, before insert) {
	//if(Trigger.isBefore){
	//	if(Trigger.isInsert){
			//Call the manageInsert method to handle Insert Event.
		//	system.debug('#######Trigger is Called###'); 
		//	PlanMilestoneTriggerHelper.manageInsert(trigger.new);
	//	}

	//}
}